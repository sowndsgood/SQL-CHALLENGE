--DRAW A TRIANGLE

--DDL
CREATE TABLE Triangle(
level INT
);

--QUERY
 WITH RECURSIVE Triangle AS(
SELECT 1 AS LEVEL
UNION ALL
SELECT LEVEL+1
FROM  Triangle
WHERE LEVEL<6
 )

 SELECT
REPEAT('*',LEVEL) AS TRIANGLE_ROW
 FROM Triangle;



